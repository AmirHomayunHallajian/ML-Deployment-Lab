# Data Notes

This repository uses fully synthetic clinical-trial site operations data.

- No real clinical, patient, or protected health information is used.
- Regenerate the dataset with: `make data` or `python scripts/generate_synthetic_data.py`.
- Processed dataset location: `data/processed/site_delay_data.csv`.
